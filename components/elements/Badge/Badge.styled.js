const badgeWrapperStyles = { position: 'relative', display: 'flex' }

const badgeStyles = {
  position: 'absolute',
  right: '-10px',
  top: '-10px',
  padding: '2px 4px',
  background: 'red',
  color: 'var(--white)',
  borderRadius: 'var(--border-radius-default)',
  fontSize: 'var(--font-size-caption)',
  lineHeight: 'var(--line-height-caption)'
}

export { badgeStyles, badgeWrapperStyles }
